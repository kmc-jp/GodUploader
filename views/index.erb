<div class="container">

  <div class="panel panel-default">
    <div class="panel-heading">
      <h2 class="text-center">最新<%= @newerillusts.count.to_s %>件の神絵</h2>
    </div>
    <div class="panel-body">
      <div class="row">
        <% @newerillusts.order( "created_at DESC" ).each do |i| %>
          <div class="col-md-3">
              <a href="<%= uri("./illust/"+i.id.to_s ,false) %>" class="thumbnail" style="height:<%= thumbheight %>;">
                <% if ishide(i) then %>
                  検閲によりサムネイル表示なし
                <% else %>
                  <img style="width:auto;height:50%;"  src="<%= uri( "./illusts/" + i.filename , false ) %>"  >
                <% end %>
              <div class="caption" style="width:auto;height:20%">
                <h2><%= i.title  %></h2>
                <p><%= i.account.name %></p>
                <p><%= i.caption %></p>
              </div>
            </a>
          </div>
        <% end %>
       </div>
    </div>
  </div>

  <div class="panel panel-default">
    <div class="panel-heading">
      <h2 class="text-center">神絵師達</h2>
    </div>
    <div class="panel-body">
      <div class="row">
        <% @accounts.each do |a| %>
          <a href="<%= uri( 'users/' + a.kmcid ,false ) %>" type="button" class="btn btn-primary col-md-3" ><%= a.name %></a>
        <% end %>
       </div>
    </div>
  </div>

</div>
